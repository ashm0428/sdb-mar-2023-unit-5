// console.log("it works from email server");

// let formDocument = document.querySelector("form");

// formDocument.onsubmit = function (e) {
//   e.preventDefault();
//   const formData = new FormData(e.target); // create a new FormData object with the form data
//   const formDataJSON = JSON.stringify(Object.fromEntries(formData)); // convert the form data to a JSON string
//   console.log(formDataJSON);
// };


